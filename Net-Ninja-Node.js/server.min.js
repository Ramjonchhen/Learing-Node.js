const http=require("http"),fs=require("fs"),server=http.createServer((req,res)=>{console.log(req.url,req.method),res.setHeader("content-type","html"),fs.readFile("./views/index.html",(err,data)=>{err?(console.log("error occured while reading the file"),res.end()):res.end(data)})});server.listen(3e3,"localhost",()=>{console.log("listening for the request on the port 3000.")});